const initialValue = {};

const userReducer = (state = initialValue, action) => {
  switch (action.type) {
    case 1:
      return;
    case 2:
      return;
    default:
      return state;
  }
};
export default userReducer;
