import { SET_CART, SET_COMPARE, SET_WISHLIST } from "../type";
const initialValue = {};

const functionReducer = (state = initialValue, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default functionReducer;
